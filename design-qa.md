# NYAWORKS 首页设计 QA

final result: passed

## 对照信息

- 视觉基准：`planning/references/home-layout-reference.png`
- 实现截图：`docs/screenshots/home-obsidian-cyan.png`
- 对照图：`docs/screenshots/home-reference-comparison.png`
- 基准尺寸：`941 × 1672 px`
- 实现截图：`941 × 1672 px`
- CSS 视口：`941 × 1672`，设备像素比 `1`
- 对照归一化：基准图与实现图使用完全相同像素尺寸并排比较
- 状态：极夜青主题、首页、主题菜单关闭
- CEP 默认窗口：`520 × 960`
- CEP 最小窗口：`420 × 640`

## 全景与重点区域

- 全景：已将基准图和实现截图合成到同一张并排对照图；顶部品牌栏、左侧导航、搜索、Banner、双九宫格与五组工具的顺序和比例已对应参考图。
- 重点区域：九宫格保持规整正方形；锚点九宫格使用九个独立命中区域；侧栏、顶部按钮与工具按钮统一为 Phosphor 线性图标。
- 交互：主题左键按既定顺序循环，右键展开五套主题菜单并可用 Escape 关闭；选择可持久化；窄面板无横向溢出。
- 语言：左键只快速切换简体中文与 English；右键打开简体中文、繁體中文、English、日本語、한국어五语言紧凑菜单，刷新后保持选择；繁体、日语、韩语状态分别使用单字符 `繁`、`あ`、`한`。
- 控制台：浏览器检查无 warning/error。

## 对比结论

- 字体与层级：品牌、标题、分组标题和工具图标层级清楚；中文使用系统 UI 字体，英文品牌使用 Bahnschrift 回退。
- 间距与布局：保持已确认的 NYAWORKS 首页结构，仅按大 / 中 / 小三档调整控件尺寸与间距；默认“中”档在 `520 × 960` 下完整显示首页功能，无需滚动。
- 颜色：默认极夜青保持蓝黑底、青色主光和少量绯粉分组标记；其他主题同步改变 Logo、Banner 与组件主光。
- 图片：Banner 使用独立高清黑猫资产，不裁切整张 UI 参考图；当前未包含参考图中的手写 `Create More`，归类为 P3 品牌装饰细节。
- 图标：所有界面图标已统一为同一套线性图标；AI 与效果采用不同图形语义。
- 文案：首页可见文案与已确认方向一致，未完成按钮均通过 Tooltip 标记为后续接入 AE。
- 多语言：当前首页、导航、顶栏、Tooltip 与占位页均已资源化；五种语言在 `520 × 960` 默认窗口中无横向或纵向溢出。

## 比较历史

1. 首轮问题：占位页取代首页、横向窗口比例、图标风格混杂、Banner 缺失。
2. 修复：重建首页视觉骨架；统一 Phosphor 图标；新增独立黑猫 Banner；恢复五组高频工具。
3. 密度优化：默认窗口改为 `520 × 960`，新增大 / 中 / 小三档界面大小基础；默认“中”档完整展示首页，切换入口后续放入完整设置页。
3. 次轮问题：九宫格在大尺寸下偏大，高频工具纵向过高，主题 Banner 未同步配色。
4. 修复：九宫格限制为参考尺寸；工具行压缩至 60px；Logo 与 Banner 随五套主题同步变色。
5. 复核：`941 × 1672` 同尺寸对照中没有剩余 P0/P1/P2 问题。

## 非本阶段验收项

- 搜索、模式切换和工具按钮仍为视觉占位。
- 未在真实 After Effects CEP 宿主中验证打开、停靠、重开和不同 AE 版本行为。
- 浏览器中的语言切换、持久化和无溢出检查不等同于真实 AE CEP 宿主验收。
- `Create More` 手写装饰可在后续品牌精修时补入 Banner 原图；本轮生成服务达到限额，因此没有用代码字体伪造。
- 尚未在真实 After Effects 中检查系统字体回退、CEP Chromium 渲染和停靠状态。
